# Gradle wrapper not yet generated. Run `gradle wrapper --gradle-version 8.10` in this folder
# to create gradlew/gradlew.bat/gradle/wrapper/gradle-wrapper.{jar,properties}.
# Then commit those files.

# Until then, builds require a system-wide Gradle 8.10 install.
